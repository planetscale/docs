const theme = {
  sizes: {
    tablet: 1200,
    phone: 750,

    // TODO: change media sizes to how many columns should be displayed
    col2: 1400,
    col1: 1200,
  },
}

export const sizes = theme.sizes
